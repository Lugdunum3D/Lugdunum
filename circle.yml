dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get dist-upgrade
    - sudo apt-get install -y clang
    - sudo apt-get install -y cmake
    - sudo apt-get install -y libc++

  override:
    - mkdir build
    - cd build && cmake ../ -DCMAKE_CXX_COMPILER=clang++ && make
