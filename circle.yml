dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y clang
    - sudo apt-get install cmake
  override:
    - mkdir build
    - cd build && cmake ../ -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ && make
